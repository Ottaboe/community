dutch mode:
    mode.enable("command")
    mode.disable("dictation")
    mode.enable("user.dutch")

dutch dictaton mode
    mode.disable("command")
    mode.enable("dictation")
    mode.enable("user.dutch")

